# Gemini Telegram Bot

Цей проєкт — простий Telegram-бот на Python. Він використовує Gemini API для генерації відповідей і може бути легко задеплоєний на [Render](https://render.com) з використанням Flask веб-сервера.

---

## 🔧 Можливості

- Приймає повідомлення від користувачів через Telegram  
- Надсилає текст в Gemini API разом з налаштованою "особистістю"  
- Відправляє відповідь назад у Telegram  
- Легка архітектура, зручно хостити на Render  
- Окремий модуль для персоналізації характеру бота
- Підтримка контексту розмови (зберігає історію повідомлень)
- Система довготривалої пам'яті для збереження важливої інформації
- Команди для управління пам'яттю бота
- Автоматичне виявлення важливої інформації в повідомленнях користувача
- Підтримка сесій розмови в групових чатах (бот продовжує відповідати учасникам розмови)
- Відправка запланованих повідомлень в чати кілька разів на день

---

## 📁 Структура проєкту

```
.
├── main.py              # Основна логіка бота і вебхука  
├── personality.py       # Налаштування особистості для промпта Gemini  
├── context_manager.py   # Менеджер контексту та пам'яті
├── scheduled_messages.py # Модуль для відправки запланованих повідомлень
├── config.json          # Конфігурація бота  
├── memory.json          # Файл довготривалої пам'яті (автоматично створюється)
├── requirements.txt     # Залежності Python  
├── README.md            # Документація проєкту  
```

---

## 🚀 Інструкція з деплою на Render

### 1. Підготуй Telegram бота

- Відкрий Telegram, напиши [@BotFather](https://t.me/BotFather)  
- Введи `/newbot`, задай ім'я та юзернейм  
- Збережи отриманий API токен

---

### 2. Завантаж код на GitHub

- Створи репозиторій на GitHub  
- Додай туди файли `main.py`, `personality.py`, `context_manager.py`, `config.json`, `requirements.txt`, `README.md`

---

### 3. Задеплой на Render

1. Зайди на [Render](https://render.com) → **New → Web Service**  
2. Підключи свій GitHub репозиторій  
3. Обери:
   - **Environment:** Python  
   - **Start Command:**
     ```
     gunicorn main:app
     ```
   - **Build Command:**
     ```
     pip install -r requirements.txt
     ```

4. Додай **Environment Variables**:
   - `TELEGRAM_BOT_TOKEN`
   - `GEMINI_API_KEY`

---

## 🌐 Webhook

Render автоматично надає HTTPS URL (типу `https://your-bot.onrender.com`).  
Встанови Telegram webhook так:

```bash
curl -F "url=https://your-bot.onrender.com/webhook" \
  https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook
```

---

## ⚙️ Структура промпта Gemini

```python
prompt = PERSONALITY + "\n\n[Memory Context]\n\n[Conversation History]\n\nUser message:\n" + user_input
```

Текст особистості визначено в `personality.py` і легко редагується.

---

## 📋 Команди бота

- `/start` - Розпочати роботу з ботом
- `/help` - Отримати допомогу
- `/schedule` - Керування запланованими повідомленнями

### Команди пам'яті

- `/memory info` - Показати збережену інформацію
- `/memory add fact <текст>` - Додати факт до пам'яті
- `/memory add topic <текст>` - Додати тему до пам'яті
- `/memory add user <ключ> <значення>` - Додати інформацію про користувача
- `/memory clear` - Очистити пам'ять для поточного чату

### Команди запланованих повідомлень

- `/schedule status` - Перевірити статус запланованих повідомлень для чату
- `/schedule on` - Увімкнути заплановані повідомлення для цього чату
- `/schedule off` - Вимкнути заплановані повідомлення для цього чату

Приклади:
```
/memory add fact Анна любить котів
/memory add topic Штучний інтелект
/memory add user ім'я Олександр
/schedule status
```

---

## 🧠 Автоматичне виявлення інформації

Бот має функцію автоматичного виявлення важливої інформації в повідомленнях користувача. Коли користувач надсилає повідомлення, система автоматично шукає:

- **Особисту інформацію**: ім'я, вік, місце проживання, хобі, інтереси
- **Теми розмови**: коли користувач виражає бажання обговорити певну тему
- **Важливі факти**: коли користувач просить запам'ятати конкретну інформацію

Інформація автоматично зберігається в пам'яті бота і використовується в подальших розмовах.

### Приклади фраз для автоматичного виявлення:

- "Мене звати Олена" → зберігає ім'я "Олена"
- "Мені подобається футбол" → зберігає інтерес "футбол"
- "Давай поговоримо про штучний інтелект" → зберігає тему "штучний інтелект"
- "Запам'ятай, що я люблю каву без цукру" → зберігає факт "я люблю каву без цукру"

## 👤 Персоналізовані враження про користувачів

Бот може формувати суб'єктивні враження про учасників розмови, базуючись на їхніх повідомленнях. Ці враження відображають те, як персонаж бота (Анна) сприймає людину, з якою спілкується.

### Як це працює:

1. Система збирає повідомлення від кожного користувача
2. Коли кількість повідомлень від користувача перевищує певний поріг (за замовчуванням 10), бот формує перше враження
3. Враження оновлюється, коли нагромаджується достатня кількість нових повідомлень (за замовчуванням 30)
4. Бот зберігає ці враження в пам'яті і використовує їх для контексту під час відповідей

### Що враховується при формуванні вражень:

- Стиль спілкування користувача
- Теми, які користувач обговорює
- Частота і тривалість повідомлень
- Манера поведінки в чаті
- Емоційний тон повідомлень

### Особливості вражень:

- Суб'єктивні, з точки зору персонажа бота
- Стислі (до 5 речень)
- Написані в неформальному стилі, характерному для персонажа
- Можуть містити як позитивні, так і нейтральні або критичні спостереження
- Природно розвиваються з часом, враховуючи нові повідомлення

### Як переглянути враження:

Використовуйте команду `/memory impressions` для перегляду всіх сформованих вражень про учасників чату.

### Налаштування функції вражень:

```json
"context_settings": {
  "user_impressions": {
    "enabled": true,                      // Увімкнути/вимкнути функцію
    "min_messages_before_impression": 10, // Мінімальна кількість повідомлень для першого враження
    "messages_between_updates": 30        // Кількість нових повідомлень для оновлення враження
  }
}
```

---

## 👥 Сесії розмови в групових чатах

Бот підтримує "сесії розмови" в групових чатах, що дозволяє продовжувати діалог з усіма учасниками після першого звернення до бота.

### Як це працює:

1. Якщо хтось звертається до бота, використовуючи ключове слово (наприклад, "Аня", "Анна"), започатковується "сесія розмови"
2. Бот автоматично відповідає на всі наступні повідомлення від учасників сесії
3. До сесії можуть приєднуватись інші учасники чату
4. Сесія автоматично закінчується після періоду неактивності (за замовчуванням 5 хвилин)
5. Сесію можна завершити вручну за допомогою команд завершення

### Команди для завершення сесії:

За замовчуванням бот розпізнає такі команди завершення сесії:
- `/end`
- "бувай"
- "допобачення"
- "дякую"

### Налаштування сесій:

Налаштування сесій можна змінити в файлі `config.json` у розділі `group_chat_settings`:

```json
"group_chat_settings": {
  "session_enabled": true,           // Увімкнути/вимкнути сесії
  "session_timeout_seconds": 300,    // Час неактивності до завершення сесії
  "auto_join_session": true,         // Автоматично додавати нових учасників
  "end_session_commands": [...],     // Команди для завершення сесії
  "auto_reply_to_session_participants": true,  // Відповідати всім учасникам сесії
  "include_reply_context": true      // Включати контекст повідомлення при відповіді на нього
}
```

## 💬 Відповіді на повідомлення (Reply)

Бот підтримує взаємодію через функцію Reply у Telegram. Коли користувач відповідає на повідомлення бота або згадує контекст іншого повідомлення через Reply, бот може автоматично враховувати цей контекст.

### Основна функціональність:

1. **Автоматична відповідь на Reply**: бот автоматично відповідає, коли користувач робить Reply на його повідомлення, навіть без згадування ключового слова.
2. **Збереження контексту**: бот додає текст повідомлення, на яке відповідають, у контекст розмови.
3. **Покращена релевантність відповідей**: завдяки збереженню контексту бот краще розуміє, на що конкретно відповідає користувач.

### Налаштування Reply-функціональності:

Налаштування можна змінити у `config.json`:

```json
"response_settings": {
  "respond_to_replies": true,        // Автоматично відповідати на Reply до повідомлень бота
  // інші параметри...
},

"group_chat_settings": {
  "include_reply_context": true,     // Включати текст повідомлення з Reply в контекст
  // інші параметри...
}
```

### Приклад роботи:

1. Користувач: "Який сьогодні день тижня?"
2. Бот: "четвер, ну або як там у вас"
3. Користувач відповідає на повідомлення бота (використовуючи Reply): "А яка зараз пора року?"
4. Бот автоматично розпізнає Reply і відповідає з урахуванням попереднього контексту.

---

## 🔐 Секрети

Всі ключі зберігай як **Environment Variables** на Render.  
Ніколи не пуш у GitHub відкриті токени.

---

## 📜 Ліцензія

MIT

## 🕒 Заплановані повідомлення

Бот може періодично надсилати повідомлення в активні чати без явного запиту користувача. Ця функція дозволяє боту:

1. Започатковувати розмови
2. Нагадувати про своє існування
3. Ділитися випадковими думками, що відповідають особистості бота

### Як це працює:

- Бот надсилає 2-3 повідомлення на день в кожен активний чат
- Повідомлення надсилаються в рандомний час
- Бот не надсилає повідомлення під час активної розмови
- Повідомлення базуються на особистості бота та можуть враховувати історію спілкування

### Типи повідомлень:

1. **Заготовлені фрази** - короткі повідомлення, що відповідають особистості бота
2. **AI-генеровані повідомлення** - унікальні повідомлення, створені з урахуванням контексту та пам'яті бота

### Налаштування:

Параметри запланованих повідомлень можна змінити в файлі `config.json` у розділі `scheduled_messages`:

```json
"scheduled_messages": {
  "enabled": true,                       // Увімкнути/вимкнути заплановані повідомлення
  "min_hours_between_messages": 4,       // Мінімальний час між повідомленнями (годин)
  "max_hours_between_messages": 12,      // Максимальний час між повідомленнями (годин)
  "max_messages_per_day": 3,             // Максимальна кількість повідомлень на день
  "active_session_cooldown_minutes": 30, // Скільки чекати після активної розмови
  "check_interval_minutes": 15           // Як часто перевіряти, чи потрібно надіслати повідомлення
}
```

## 🔄 Обробка пересланих повідомлень у пакетному режимі

Бот може виявляти і обробляти групи повідомлень, які надсилаються одночасно (наприклад, переслані повідомлення), відповідаючи на них як на єдине ціле, а не окремо на кожне повідомлення.

### Як це працює:

1. Коли користувач надсилає повідомлення з ключовим словом, бот короткий час (за замовчуванням 2 секунди) очікує додаткових повідомлень від того ж користувача
2. Якщо протягом цього часу надходять додаткові повідомлення, вони групуються разом
3. Бот генерує єдину відповідь на всю групу повідомлень замість окремих відповідей на кожне
4. Це особливо корисно, коли користувачі пересилають кілька повідомлень з інших чатів

### Переваги:

- Зменшення спаму в чаті
- Економія API запитів до Gemini
- Більш контекстні відповіді, що враховують всі переслані повідомлення разом
- Природніша поведінка бота

### Налаштування:

```json
"message_batching": {
  "enabled": true,                // Увімкнути/вимкнути функцію пакетної обробки повідомлень
  "timeout_seconds": 2,           // Скільки секунд чекати додаткових повідомлень
  "combine_forwarded_messages": true // Дозволити комбінувати переслані повідомлення
}
```

Якщо функція вимкнена, бот відповідатиме на кожне повідомлення з ключовим словом окремо, як раніше.
